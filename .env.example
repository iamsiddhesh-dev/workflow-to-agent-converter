# Gemini AI Studio free-tier key (primary translator + runtime LLM)
GEMINI_API_KEY=

# Groq free-tier key (fallback on 429/5xx)
GROQ_API_KEY=

# Mock mode for generated crews — set to 1 to use canned MockLLM responses (zero cost)
MOCK_MODE=0
