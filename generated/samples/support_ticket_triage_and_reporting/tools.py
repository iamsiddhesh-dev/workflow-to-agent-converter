"""Generated by w2a — tools for "Support Ticket Triage and Reporting".

2 tool(s) declared in the spec. Tools that matched the
closed builtin registry are real, tested implementations copied in verbatim;
everything else is an explicit MOCK_MODE stub with a TODO. The generator
never invents an import or API name — if a tool isn't listed here, the
workflow doesn't have it.
"""

from __future__ import annotations


from crewai.tools import tool

from config import MOCK_MODE


@tool('Zendesk Connector')
def zendesk_api(input_data: str) -> str:
    'Provides read access to incoming ticket content and allows for the updating of ticket labels after triage classification.'
    if MOCK_MODE:
        return 'MOCK: ticket content and current metadata'
    # TODO: connect real Zendesk Connector
    raise NotImplementedError('Zendesk Connector is not wired to a real system yet')


@tool('PagerDuty Notifier')
def pagerduty_api(input_data: str) -> str:
    'Enables the transmission of urgent alerts to the on-call staff when critical issues are identified in the support queue.'
    if MOCK_MODE:
        return 'MOCK: API success confirmation'
    # TODO: connect real PagerDuty Notifier
    raise NotImplementedError('PagerDuty Notifier is not wired to a real system yet')


ALL_TOOLS = [zendesk_api, pagerduty_api]
