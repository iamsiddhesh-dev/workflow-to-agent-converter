"""Generated by w2a — tools for "PR Summary Generator".

2 tool(s) declared in the spec. Tools that matched the
closed builtin registry are real, tested implementations copied in verbatim;
everything else is an explicit MOCK_MODE stub with a TODO. The generator
never invents an import or API name — if a tool isn't listed here, the
workflow doesn't have it.
"""

from __future__ import annotations


from crewai.tools import tool

from config import MOCK_MODE


@tool('Git Diff Reader')
def git_diff_reader(input_data: str) -> str:
    'Retrieves the raw code changes from a pull request to enable the diff analyst to examine the scope of work.'
    if MOCK_MODE:
        return 'MOCK: raw diff text'
    # TODO: connect real Git Diff Reader
    raise NotImplementedError('Git Diff Reader is not wired to a real system yet')


@tool('PR Commenter')
def pr_commenter(input_data: str) -> str:
    'Publishes the generated summary or one-liner directly to the pull request thread to improve the review process.'
    if MOCK_MODE:
        return 'MOCK: confirmation of post'
    # TODO: connect real PR Commenter
    raise NotImplementedError('PR Commenter is not wired to a real system yet')


ALL_TOOLS = [git_diff_reader, pr_commenter]
