# {{ workflow.name }}

Generated by w2a from a `{{ pattern }}` pattern.

{{ workflow.description }}

**Trigger:** {{ workflow.trigger }}

## Pattern notes

{{ pattern_notes }}

## Agents
{% for a in agents %}
- **{{ a.role }}** (`{{ a.id }}`) — {{ a.goal }}
{% endfor %}

## Tasks
{% for t in tasks %}
- `{{ t.id }}` (agent: `{{ t.agent_id }}`){% if t.depends_on %} — depends on {{ t.depends_on | join(', ') }}{% endif %}{% if t.human_checkpoint %} **[human checkpoint]**{% endif %}
  {{ t.description }}
{% endfor %}

## Tools
{% if tools %}
{% for tl in tools %}
- **{{ tl.name }}** ({{ tl.category }}) — {{ tl.purpose }}. Currently a `MOCK_MODE` stub in `tools.py` — real wiring is Phase 4's job (`# TODO: connect real {{ tl.name }}`).
{% endfor %}
{% else %}
None declared by the spec.
{% endif %}
{% if assumptions %}

## Assumptions carried from the spec
{% for a in assumptions %}
- {{ a }}
{% endfor %}
{% endif %}

## Running it

```
pip install -r requirements.txt
cp .env.example .env   # fill in GEMINI_API_KEY (or GROQ_API_KEY)
MOCK_MODE=1 python main.py path/to/input.txt   # dry run, no API calls, no key needed
python main.py path/to/input.txt               # real run
```

What's real: the crew wiring in `crew.py`, the LLM plumbing in `config.py`.
What's stubbed: every tool in `tools.py` until Phase 4 resolves it against the
built-in registry or leaves it as an explicit mock with a TODO.
